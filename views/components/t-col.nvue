<template>
    <view class="t_col_box flex flex-ac jc-sb" :class="customClass" @click="clickR">
        <view class="col_title" :class="props.customWidth ? 'customWidth' : ''"><text class="title_txt">{{ data.title }}</text></view>
        <view class="col_right flex flex-ac flex-1 jc-end">
            <view :class="{ 'img_bg': true, 'img_bg_circle': data.imageIsCircle }" v-if="data.type == 'image'">
                 <image class="set_img" :src="data.value || data.defaultValue" :class="{ 'set_img': true, 'img_set_circle': data.imageIsCircle }"></image>
            </view>
            <view class="set_content_box flex-1" v-else>
                <text class="txt_dcm nowrap" :class="rTextClass">{{ data.text || data.value || data.defaultValue }}</text>
            </view>
            <image
                :src="imgUrlEvent('/images/mine/arrows2.png')"
                alt=""
                class="order_arrows"
            ></image>
        </view>
    </view>
</template>

<script setup lang="ts">
import { imgUrlEvent } from "@/infrastructure/utils/imgUrls";
import { ref, defineProps, defineEmits } from "vue";
const props = defineProps(["data", "custom-class", "r-text-class", "custom-width"]);
const emits = defineEmits(["edit-click"]);

// 触发点击
function clickR() {
    emits('edit-click', props.data)
}
</script>
 
<style lang="less" scoped>
.t_col_box {
    border-bottom: 2rpx solid #EEE;
    min-height: 104rpx;
}
.is_no_border {
    border-bottom: none;
}
.col_title {
    width: 240rpx;
}
.customWidth {
    width: auto;
    min-width: 500rpx;
}
.title_txt {
    font-size: 28rpx;
    color: #111111;
    font-style: normal;
    font-weight: 500;
}
.col_right {
    color: #777777;
    text-align: right;
    font-size: 24rpx;
    font-style: normal;
    font-weight: 400;
    // margin-left: 20rpx;
    padding: 32rpx 0 32rpx 20rpx;
}
.img_bg {
    // background: #ccc;
    border-radius: 8rpx;
    border: 2rpx solid #f7f6f6;
    // margin: 32rpx 0;
}
.img_bg_circle {
    border-radius: 36rpx;;
}
.set_img {
    width: 132rpx;
    height: 72rpx;
    border-radius: 8rpx;
}
.img_set_circle {
	width: 72rpx;
    border-radius: 36rpx;
}
.set_content_box {
    text-align: right;
}
.txt_dcm {
    text-align: right;
    color: #777777;
    font-size: 24rpx;
    font-style: normal;
    font-weight: 400;
}
.order_arrows {
    width: 24rpx;
    height: 24rpx;
    margin-left: 32rpx;
}
.g_clr {
    color: #1CDAFE;
}

</style>
